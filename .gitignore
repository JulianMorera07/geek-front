# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.claude/

# Android TWA (Bubblewrap), vive en android-twa/ (aparte de src/app/ del
# App Router de Next — un `app/` en la raíz choca con la convención de Next y
# rompe el build, confirmado en vivo). El proyecto fuente
# (twa-manifest.json, app/, build.gradle, gradlew, etc.) SÍ se commitea, lo
# necesita el workflow de CI para compilar. Lo que NUNCA debe ir al repo:
android-twa/android.keystore
android-twa/android.keystore.b64
# Ojo: un `*.apk`/`*.aab` global acá casi rompe la app en producción — matchea
# CUALQUIER path que termine así, incluida la carpeta de la ruta real
# `src/app/download/geekbaku.apk/route.ts` (Next la trata como un segmento de
# URL, no como un archivo) — Git la ignoraba en silencio, nunca llegaba a los
# commits ni al deploy aunque `git status` se viera limpio. Acotado a las
# rutas reales donde cae el binario compilado.
android-twa/*.apk
android-twa/app/build/**/*.apk
*.aab
android-twa/app/build/
android-twa/.gradle/
android-twa/hs_err_pid*.log
