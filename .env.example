# URL base del backend real (incluye el prefijo /api/v1).
# Se usa tanto en servidor (Server Components) como en cliente (React Query),
# por eso lleva el prefijo BACKEND_INTERNAL_.
BACKEND_INTERNAL_URL=http://localhost:8000/api/v1